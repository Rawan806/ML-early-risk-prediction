# ML-early-risk-prediction
Early health risk prediction using supervised ML on tabular clinical features. Includes data preprocessing, baseline models, tuned classifiers, evaluation metrics (ROC-AUC/F1), and model interpretability.
